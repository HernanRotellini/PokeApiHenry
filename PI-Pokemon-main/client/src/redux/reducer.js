

const initialState= {
allPokemons:[],
pokemonDetail:{},
orderedPokemons:[],
filteredPokemons:[]
}

const reducer = (state= initialState, action)=>{
    switch(action.type){
        case "nn":{
            break
        }
        default: {
        return}
    }
}